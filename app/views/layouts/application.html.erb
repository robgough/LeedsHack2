<!DOCTYPE html>
<html>
<head>
  <title>Weighted Down</title>
  <%= stylesheet_link_tag :all %>

  <%= javascript_include_tag :defaults %>
  <%= javascript_include_tag "jquery.flot.min.js" %>

  <%= csrf_meta_tag %>
  <meta name="viewport" content="width=device-width,initial-scale=1,user-scalable=no" /> 
  <link href='http://fonts.googleapis.com/css?family=Muli' rel='stylesheet' type='text/css'>
	<% if Rails.env.production? %>
		<script type="text/javascript">

		  var _gaq = _gaq || [];
		  _gaq.push(['_setAccount', 'UA-25844113-1']);
		  _gaq.push(['_trackPageview']);

		  (function() {
		    var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
		    ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
		    var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
		  })();

		</script>
	<% end %>

</head>
<body>
<div id="container">

<nav>
	<% if current_user %>
		<%= current_user.email %>
		<%= link_to "Dashboard", dashboard_path %> |
		<%= link_to "Profile", user_path(current_user) %> |
		<%= link_to "Settings", settings_users_path %> |
		<%= link_to "Logout", logout_path %>
	<% else %>
		<%= link_to "Join", join_path %> or 
		<%= link_to "Login", login_path %>
	<% end %>
</nav>

	<% flash.each do |name, msg| %>
		<%= content_tag :div, msg, :id => "flash_#{name}" %>
	<% end %>

	<%= yield %>

	<div id="footer">
		Have a problem or just want to say hello? <a href="mailto:rob@weighteddown.co.uk">Send me an email</a><br />
		Keep up to date by visiting <a href="http://blog.weighteddown.co.uk">the blog</a></br />
		or <a href="http://twitter.com/weighteddownapp">follow us on twitter</a></br>
		This site is currently in beta<br />
		Version 0.1.5<br />
		Made by <a href="http://robgough.net">Rob Gough</a>
		<%= debug(params) if Rails.env.development? %>
	</div>
</div>
</body>
</html>
